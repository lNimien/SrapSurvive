import { handlers } from '../../../../server/auth/auth';

// Auth.js v5: this file is the catch-all route for all auth endpoints
// (sign-in, sign-out, callback, session, etc.)
export const { GET, POST } = handlers;
